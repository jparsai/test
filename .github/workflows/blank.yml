name: CI
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:
jobs:
  build:
    runs-on: docker pull quay.io/jparsai/crdaimagev1

    steps:
      - uses: actions/checkout@v2

      - name: Task_1
        run: cat /etc/os-release

  my_job:
    runs-on: ubuntu-latest
    container:
      image: quay.io/jparsai/crdaimagev1:latest
    steps:
      - run: echo "Runs inside a container"
